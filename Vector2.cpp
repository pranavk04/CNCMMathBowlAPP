#include "Vector2.h"

// defines the function void vector2::set(x,y), which by using pointers itself it sets the vectors x and y values as those specified. 

void vector2::set(float x, float y)
{
	this->X = x;
	this->Y = y;
};